# Workers

Empty scaffold. Phase 9 ("Daily Autonomous Job Agent") implements the Celery
worker/beat processes here, backed by Redis as the broker. No worker code or
container runs yet — this directory just reserves the seam so later phases
don't need to restructure the repo.
